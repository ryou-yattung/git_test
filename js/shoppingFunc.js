const user = {
  name: "TTT",
  active: true,
  cart: [],
  purchases: [],
};

function purchaseItem (user,item){
  return Object.assign({},user ,{purchases:item})
}

function itemToCart()

function applyTaxToItems()

function buyItem()

function empyCart()
